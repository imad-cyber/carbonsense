apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-pvc
  namespace: carbonsense
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vector-store-pvc
  namespace: carbonsense
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 2Gi
